# Current State

## Last Updated
2026-07-26

## Current Project Phase
Active Development / Polishing

## Current Objective
-15. User reported "Generate Content" progress stuck at 0%. Root cause this time was purely operational, not a code bug: the `apps/backend` BullMQ worker process simply wasn't running (only the frontend dev server was up) - confirmed via `ps aux` and an actual stalled `Job` row (id 24, `status: 'running'`, `progress: 0`, created that session). Starting the backend immediately let it pick up the queued job from Redis and it completed normally (3 rows, all hit the already-known-0-quota `gemini-pro-latest` model and errored, but the job itself completed/wasn't stuck). No code change for this part - just confirms `npm run dev --workspace=@venture27/backend` needs to be running for generation to progress at all, and it doesn't auto-start with the frontend.
  - Follow-up feature request from the same conversation: show live generated/error counts below the progress bar. This needed a small, deliberately-flagged Prisma schema change (`PROJECT_CONTEXT.md` lists "The Prisma Database Schema" under changes requiring explicit approval) - asked via `AskUserQuestion` (schema-backed accurate per-run counts vs. a no-migration category-wide approximation); user chose the schema-backed option. Added `Job.generatedCount`/`Job.errorCount` (`Int @default(0)`) via `prisma db push` (clean, additive, no data loss). `apps/backend/src/index.ts`'s per-item loop (`runGeneration`) now does `prisma.job.update({ data: { progress, ...(succeeded ? { generatedCount: { increment: 1 } } : { errorCount: { increment: 1 } }) } })` in the same call as the existing progress update. `apps/frontend/app/generate-content/page.tsx` tracks new `generatedCount`/`errorCount` state (reset to 0 on `startGeneration`, populated from the job response in both `reattachActiveJob` and the polling loop), rendered as "N generated" (green dot) / "N error" (red dot) directly under the progress bar, only shown once at least one item has been attempted.
  - Verification caught two real process-hygiene issues worth remembering for next time, both are instances of the same underlying gotcha: **neither the Next.js dev server nor the `tsx` backend process hot-reloads what matters here** - (1) after `prisma db push` regenerates `@prisma/client`, an already-running Next.js dev server keeps using the OLD generated client from memory/its module cache, so the new `generatedCount`/`errorCount` fields were silently missing from `GET /api/generate` responses until the frontend was restarted; (2) `apps/backend`'s `tsx src/index.ts` has no watch mode (a pre-existing known issue, see below), so after editing the increment logic into `runGeneration`, the OLD backend process (started earlier in the session, before the edit) kept running and a real test job completed with both counts staying 0 - not a logic bug, just stale code still executing. Both required an explicit process restart to pick up the change; a synthetic `Job` row inserted directly via `psql` (then deleted) was used first to verify the API/UI wiring independent of these two gotchas, followed by two real end-to-end generation runs (`categoryId: 6` "SEO", `aiModel: gemini-pro-latest`, `limit: 1` - deliberately using the already-known-0-quota model so the real test cost nothing) to confirm the actual worker increments correctly once the fresh code was actually running. `npx tsc --noEmit` clean (frontend); backend has no `tsconfig.json`/type-check step (pre-existing, `tsx` doesn't type-check).
-14. Follow-up to item -13: user reported the footer's column gaps looked too narrow after the reorder, asked to match the mockup. Fetched real layout data for the footer node (`26308:4788`, depth 2) to get exact column x-positions rather than guessing - confirmed the mockup's spacing is asymmetric: 142px between the Company/logo column and Services, but only 28px between Services/Venture27/Legal. This couldn't be done with the previous `display: grid; grid-template-columns: repeat(4, 1fr)` layout - a grid item's `margin-right` doesn't push the next track over, since track positions are fixed by the grid regardless of item margins - so `.ppage-footer-inner` was switched to `display: flex; flex-wrap: wrap; gap: 28px` (28px = the real Services-Venture27-Legal spacing) with `.ppage-footer-company` given `margin-right: 114px` (114 + the base 28px gap = 142px total, matching Figma exactly) and a fixed `flex: 0 0 340px` width matching the mockup's real Company column width; the other three columns flex-grow equally. Verified via `getBoundingClientRect()` on all four `.ppage-footer-col` elements: gap 1→2 measured 142px, consistent ~28px thereafter. `tsc --noEmit` clean.
-13. Follow-up to item -10's footer: user pointed out the logo/company block should be on the LEFT of the public page's footer, not the right where the Figma mockup originally placed it (mockup order was Services/Venture27/Legal/Company). Reordered the JSX in the single-service page so `.ppage-footer-company` renders first, before the `FOOTER_COLUMNS` map - a deliberate, explicit deviation from the Figma source order, not a bug fix. Verified via Playwright (`boundingBox()` on the logo: x moved from 1086 to 24, i.e. first grid column). `tsc --noEmit` clean.
-12. Follow-up to items -10/-11: user asked to also re-theme the admin dashboard to match the new Figma brand ("sesuaikan juga style di dashboard lalu logo nya update juga"). Scoped this to a values-only recolor rather than a markup/component rewrite, since the dashboard's existing dark backgrounds (`#0a0e17` etc.) were already close to the Figma page's `#090A0E` - the real mismatch was the accent color (blue `#3b82f6` vs the Figma brand teal `#5BEBCD`).
  - `apps/frontend/app/globals.css`: the `--color-blue-50..900` scale (still named "blue" - see below) was recolored to a teal scale anchored on the exact Figma accent at the 400 step (`#5bebcd`), `--color-accent`/`--color-accent-hover` now `#2fd1b0`/`#1fb89a` (the 500/600 steps). Every hardcoded literal `rgba(59, 130, 246, X)` in the rest of the file (17 occurrences - sidebar active nav item, stat card hover glow, primary button, upload-area hover/dragover, form-input focus ring, variable-tag, generation-status-icon running pulse, etc.) was replaced with `rgba(91, 235, 205, X)` via a single `sed` pass (verified 0 occurrences of the old triplet remained). One more hardcoded instance in `app/result-guide/page.tsx` (a code-sample background tint) fixed the same way.
  - **Deliberately kept the CSS variable names as `--color-blue-*`** rather than renaming to `--color-teal-*`, even though they no longer hold blue values - `apps/frontend/app/result-guide/page.tsx`, `apps/frontend/app/generate-content/page.tsx`, and `apps/frontend/app/components/ServiceListPage.tsx` all reference `var(--color-blue-300)`/`var(--color-blue-400)` directly inline; renaming the variables would have required touching every one of those call sites for a purely cosmetic rename, so the historical name was kept with a comment explaining why, and only the values changed. Anyone touching this again: `--color-blue-400` IS the brand teal, not a mistake.
  - **Logo**: replaced the sidebar's old text-only placeholder badge (`<div className="sidebar-brand-icon">V27</div>`, a gradient box with literal "V27" text) in `apps/frontend/app/components/Sidebar.tsx` with the real downloaded Venture27 wordmark PNG. Moved the logo asset from `apps/frontend/public/service-page/venture27-logo.png` (where item -10 originally put it, service-page-scoped) to `apps/frontend/public/venture27-logo.png` (shared top-level, since it's now used by both the admin sidebar AND the public page) - updated both `img src` references in the public single-service page accordingly, and removed the old `sidebar-brand-icon`/`sidebar-brand-text h1` CSS rules (the wordmark logo image already spells "VENTURE27", so the redundant "Venture 27" `<h1>` text next to it was dropped, keeping only the "Data Generator" subtitle).
  - Verified live via Playwright screenshots of `/` (Overview) and `/generate-content`: sidebar logo renders at the correct un-stretched aspect ratio (155×28, confirmed via `boundingBox()`), active nav item/primary buttons/links/icons all render in the new teal consistently across both pages (stat card icons, "Continue Generation" primary button, "Configure in Settings" link, active sidebar state). `npx tsc --noEmit` clean. Did not re-screenshot every other admin page individually - the change is a shared CSS-variable/value swap (not per-page markup), so the same recolor applies uniformly everywhere `var(--color-accent)`/`var(--color-blue-*)` is already used; the two screenshotted pages between them exercise most of the shared component classes (cards, badges, buttons, inputs, dropdowns, nav).
-11. Follow-up to item -10: added the mockup's "Request Estimate" lead-capture form into the hero's right column (was skipped in the first pass per the user's own earlier instruction to leave it out - they later asked for it back in, "tambahkan form di bagian sidebarnya seperti di mockup"), and fixed a real footer-logo distortion bug the user spotted ("gambar logo terlihat melar lalu posisinya juga salah").
  - **Layout restructure**: `.ppage-main` changed from a single centered column (max-width 900px) to a two-column flex row (max-width 1400px) - left column (`.ppage-main-col`, unchanged content: breadcrumb/eyebrow/h1/subheading/article) plus a new right column, the form card, `position: sticky; top: 96px` so it stays visible while scrolling the long article (a CSS-only enhancement, not in the static Figma export literally, but standard/expected for this exact template shape - collapses to a static full-width block below the content on narrow viewports via `@media (max-width: 1100px)`). This matches the mockup's actual node structure more faithfully than the first pass did - the subagent's original extraction had already flagged that the form's root frame (`Service` `#26315:25867`) is a sibling of the left content column inside the SAME two-column row, not a separate section.
  - **Form contents**: fetched fresh, scoped `get_figma_data` on just node `26315:25867` (the previous whole-page fetch was too large/summarized to have exact copy) - exact heading "Request Estimate", intro copy, and all 5 field labels/placeholders (`Name *`/"Full Name", `Email *`/"email@domain.com", `Phone`/country-code selector+"xxxx-xxxx-xxxx", `Company`/"Company Name", `How Can We Help? *`/textarea "Your message...") plus submit button text "Request an Estimate" are verbatim from Figma. Downloaded 2 more icons (`icon-flag-us.svg`, `icon-caret-down.svg`) for the phone field's country selector into `apps/frontend/public/service-page/`.
  - **Deliberately NOT wired up**: this app has no lead-capture/CRM backend or schema for storing form submissions, and building one wasn't asked for - so the form is real semantic markup (`<label>`/`<input>`/`<textarea>`) for visual/accessibility fidelity, but the submit control is a plain `<button type="button">` with no `onClick`/`action`, consistent with the nav/footer links elsewhere on this page also being intentionally inert until real destinations/backends exist. If a future session is asked to make it actually submit somewhere, that's a distinct, larger task (new API route + schema at minimum).
  - **Footer logo bug root cause**: `.ppage-footer-col` is `display:flex; flex-direction:column` with the default `align-items: stretch`, which was stretching the `<img>` logo to fill the column's full width regardless of its `width: auto` CSS (measured via Playwright's `boundingBox()`: logo rendered at 330×32 instead of the correct ~198×32 for the source PNG's actual 1582×256 aspect ratio - confirmed distorted, not a false alarm). Fixed by giving both the footer logo AND (defensively) the navbar logo explicit `width`+`height` matching the real aspect ratio (173×28 and 235×38 respectively) plus `align-self: flex-start` on the footer one, instead of relying on `height` + `width: auto` inside a stretching flex container. Re-verified via `boundingBox()` after the fix: 173×28 exactly as intended.
  - Verified live via Playwright screenshot (same real published row as item -10) after each fix: form renders pixel-faithful to the Figma copy including the flag/caret icons, footer logo no longer stretched. `npx tsc --noEmit` clean.
-10. Redesigned the public single-service page (`app/[type]/services/[value]/[service]/[heading]/page.tsx`) to match a Figma mockup, via the Figma Dev Mode-style MCP server (`figma-developer-mcp`, connected through `/Users/nogariantana/File/.mcp.json` - note: the actual open workspace root is `/Users/nogariantana/File`, one level above this `venture27` repo, so project-scoped MCP/settings config must live there, not inside the repo). Source mockup: Figma file `9XNgcaxug2WRsjikyfEixG`, node `26308:4557` ("Programmatic SEO template"), a generic agency-marketing template themed around one example service ("Mobile App Development in Calgary, Alberta").
  - Per explicit user decisions (asked via AskUserQuestion, since these were genuine judgment calls, not obvious from the mockup): (a) the mockup's "How We Help" / "Services Grid" / "Why Choose Us" mid-page sections contain copy hardcoded to the mobile-app example and don't generalize to this app's real, varied services (SEO, self storage, etc.) - so only their VISUAL treatment was kept (h2 section dividers with a top border + generous padding, teal `#5BEBCD` dot-bullet `<li>` styling) and applied via CSS to the REAL per-row AI-generated article (`item.content`), rather than reproducing the mockup's specific fake sub-sections/icon-grids, which would require restructuring the AI generation prompt/schema (out of scope). (b) The mockup's navbar (`Home/Services/Our Approach/Why Choose Us/Recent work/Contact`) and footer link columns point to pages that don't exist in this app (there's no public marketing site, just this one page type) - kept visually per the mockup but rendered as inert `<span>` elements (not `<a>`), consistent across nav/CTA button/back-link/footer links.
  - Explicitly skipped, per direct instruction ("sisakan saja bagian form dan faq nya"): the mockup's right-column lead-capture form (`Service` frame `#26315:25867`) and the FAQ accordion (`#26309:6210`) were not rebuilt at all - hero is now a full-width single column (H1 + subheading only, no reserved form space).
  - One real content-fidelity fix made while translating: the mockup's Figma source has a "Legal" footer column that's actually a duplicate of the "Venture27" column's 4 links (`About/Contact/Request A Call/Request Estimate`) followed by only 3 genuinely-legal items - this reads as an artifact/bug in the source file, so the built "Legal" column uses only the 3 real items (`Blog`, `Privacy and Policy`, `Terms and Conditions`) rather than reproducing the duplication.
  - Assets: downloaded the Venture27 logo (PNG) + 10 icon SVGs (back-arrow, 5 benefit icons, 4 service icons) via `download_figma_images` into `apps/frontend/public/service-page/` - only the logo and back-arrow icon ended up used in the final page (the benefit/service icons belonged to the skipped mid-page sections' original grid layout); left the unused ones in place rather than deleting, in case a future session wants to build those sections out for real.
  - Hit and fixed one real bug during verification: initial version used `<a href="#" onClick={(e) => e.preventDefault()}>` for the inert nav/footer/CTA/back links, which threw a Next.js Server Component error at runtime ("Event handlers cannot be passed to Client Component props") since this page has no `'use client'` directive and was never intended to need one (fully static per-row render). Fixed by switching all of those to plain `<span>` elements with the same CSS classes (visually identical, no event handler needed since they were never meant to be clickable). Caught via an actual Playwright screenshot (installed Chromium fresh this session, `~/Library/Caches/ms-playwright`), not just `tsc --noEmit` - the type-check passed cleanly both before and after this bug, since the prop is technically well-typed, it's a runtime-only RSC constraint.
  - Verified live end-to-end via Playwright screenshot against a real published row (`/city/services/coal-harbour/seo-for-appliances-repair/...`, real AI-generated SEO content, not test data): navbar/hero/eyebrow/breadcrumb/article typography+bullets/footer all render matching the mockup's visual language, with genuinely varied real content (confirms the "style-only, content-agnostic" approach generalizes beyond the mobile-app example). `npx tsc --noEmit` clean.
-9. Fixed a "generation looks stopped after navigating away" report on `/generate-content` ("saat saya pindah halaman dan kembali lagi sepertinya prosesnya malah berhenti"). Root cause: generation already runs entirely server-side in the `apps/backend` BullMQ worker, independent of the frontend - navigating pages (or even closing the browser) never called `stopGeneration`/any stop action (confirmed by grepping for `beforeunload`/`stopGeneration`/`visibilitychange` usage - none exist outside the Stop button's own `onClick`). The real bug was purely client-side: `/generate-content` is a React component whose `isGenerating`/`jobId`/`progress` state lives only in memory, so navigating away and back remounts it fresh (`isGenerating: false`, `jobId: null`) and the UI falls back to showing the "Start/Continue Generation" button instead of the progress bar - even though the job was still actively processing rows in the background the whole time. Fixed by adding a reattach mechanism: `GET /api/generate` (in `apps/frontend/app/api/generate/route.ts`) now supports being called with no `jobId` - it returns the most recent `Job` row with status `running` or `paused` (previously this case just 400'd with "jobId required"). `apps/frontend/app/generate-content/page.tsx` calls this on mount (new `reattachActiveJob()`, alongside the existing `fetchData`/`fetchPrompts` calls in the initial `useEffect`) and rehydrates `jobId`/`progress`/`isGenerating`/`isPaused` from whatever it finds, which in turn re-triggers the existing polling `useEffect`. `npx tsc --noEmit` clean. Verified live end-to-end: inserted a synthetic `Job` row (`status: 'running', progress: 42`) directly via `psql` and confirmed `GET /api/generate` (no `jobId`) returned it; confirmed a `completed` job correctly returns nothing to reattach to. Also live-confirmed the underlying premise while restarting the backend for this test: a real job the user had started earlier (`Job` id 22, category with 255 pending rows, Gemini) was still enqueued in BullMQ/Redis and auto-resumed processing the instant the backend restarted, proving the architecture already survives restarts/page navigation/browser close - this was purely a UI reattachment gap, not a backend bug.
    - Side effect of that verification worth recording: restarting the backend to test this **actually resumed the user's real, real-money-adjacent generation job** (their real Gemini key), which then hit free-tier rate limits (`429 RESOURCE_EXHAUSTED`, `generate_content_free_tier_requests` limit 20/day for `gemini-flash-latest`... actually `gemini-3.6-flash` per the live error - Google's `-latest` alias had rolled to a newer underlying model than expected, worth knowing next time quota math is discussed) before the user was asked and explicitly chose to stop it (`AskUserQuestion` → "Stop job ini"). Job 22 set to `status: 'stopped'` directly via `psql` (mirrors what the Stop button does) and confirmed via the backend log (`Job 22 was stopped.`) that the worker's per-item status check picked it up cleanly on the next loop iteration rather than being killed mid-request. Final state left for the user: of that category's 260 rows, 5 `generated`, 14 `error` (quota-exhaustion errors, safely retryable later via the existing per-row/"Reset Errors" UI once quota resets), 241 still `pending` - nothing lost, resumable anytime via "Continue Generation".
    - This also reconfirms (see "Known Problems" below) that BullMQ's stalled-job redelivery-on-restart behavior is real and not just a rare edge case from force-killing mid-run - it fired again here from an ordinary `npm run dev` restart. Worth the next AI keeping in mind when restarting the backend during active development: if a real job is `running`, a plain restart resumes it (and its real API calls) rather than pausing it.
-8. Docker now auto-restores the database from the user's dump. The user pushed their `venture27.dump` (pg_dump CUSTOM format, PG 15.18, dumped as role `postgres`, contains both `public` and `pgboss` schemas) into a Docker image `rakawebmlg/venture27-dbdump:latest` at `/venture27.dump`, and asked for `docker-compose.yml` to be adjusted accordingly. Because it is custom format, Postgres' `docker-entrypoint-initdb.d` cannot execute it directly (it only auto-runs `.sql`/`.sql.gz`/`.sh`), so the restore goes through a `pg_restore` wrapper script. New wiring: a one-shot `dbdump` init service (`platform: linux/amd64` — the image is amd64, the user's Mac is arm64) copies the dump into a shared named volume `dumpdata`; `db` mounts `dumpdata` read-only at `/dump` and gates on `depends_on: dbdump: condition: service_completed_successfully`; `./docker/initdb` is mounted at `/docker-entrypoint-initdb.d`, where new `docker/initdb/10-restore-dump.sh` runs `pg_restore --no-owner --no-privileges --exit-on-error`. Restore therefore only ever happens on an EMPTY `pgdata` volume (fresh `up`, or after `down -v`) — it can never clobber a live database. Healthchecks added to `db` and `redis`; the obsolete `version: '3.8'` key dropped. Verified live: restore ran clean and the data the docs below describe as lost is BACK — Category 3, Location 20, Service 13, MasterData 260, PromptTemplate 2, Job 17, Settings 1, sequences restored (`MasterData_id_seq` = 802), and `prisma migrate diff` against the restored DB reports "No difference detected" versus `schema.prisma` (so no `db push` is needed). Also recreated `packages/database/.env` (gitignored, missing from this clone, so nothing could connect) plus a committed `.env.example`, both pointing `DATABASE_URL` at the compose credentials.
-7. Fixed TS "red indicator" errors the user flagged across several files (`di beberapa file ada indicator merah`). Root cause: `array.reduce((acc, item) => {...}, {} as Record<...>)` on an `any[]`-typed array hits a TS generic-inference bug where the callback's `acc`/the final result infers as `unknown` instead of the cast type (confirmed via an isolated repro - moving the initial value to an explicit `as` cast, as opposed to passing an explicit generic, defeats inference here). Fix: use an explicit generic on `reduce`, e.g. `filteredData.reduce<Record<string, typeof filteredData>>((acc, item) => {...}, {})`, dropping the trailing `as` cast. Applied to `app/import/page.tsx`, `app/master-data/page.tsx`, `app/generate-content/page.tsx` (both reduces), `app/components/ServiceListPage.tsx`. Two unrelated errors also found and fixed while running a full `tsc --noEmit`: `app/history/page.tsx` referenced `job.error_message` which didn't exist on the mock job type (added the field to `app/lib/mockData.ts`); `apps/frontend/worker.ts` (dead pg-boss code, previously flagged in Pending) failed to type-check on a missing `pg-boss` dependency - confirmed unused anywhere (not in any `package.json` script, not imported by any file) and deleted outright rather than fixed. `npx tsc --noEmit -p tsconfig.json` is now fully clean.
-6. SEO + sitemap + robots.txt for the public single-service page, per explicit request. Public page gained canonical/OG/Twitter/robots meta (via `metadataBase` now set in root layout, env var `SITE_URL`) and a JSON-LD `Service` block. New chunked sitemap: `app/lib/sitemap.ts#getSitemapChunks()` groups published rows by slug type (city/community/county) into <=10,000-URL chunks named `{type}-{start}-{end}.xml`; `GET /sitemap-index.xml` lists them, `GET /sitemaps/{filename}` serves one. `app/robots.ts` disallows everything except `/city/`, `/community/`, `/county/` and points at the sitemap index.
-5. Restructured Service navigation and the public slug format, per explicit follow-up ("rubah saja menjadi sub... slugnya akan disesuaikan"). `/services` no longer exists as one page with in-page grouping - it's now three sidebar sub-menu items (`/services/city`, `/services/community`, `/services/county`, each a thin wrapper around new `app/components/ServiceListPage.tsx`), and `/services` itself redirects to `/services/city`. The public page slug changed from `/{place}/services/{category}/{service}` to `/{type}/services/{value}/{service-name}/{heading}` (type = whichever of city/community/county is primary - see `lib/location.ts#primaryLocationType` - Category dropped, a rendered Heading segment added). The old `app/[city]/services/[category]/[service]/page.tsx` route was deleted and replaced by `app/[type]/services/[value]/[service]/[heading]/page.tsx`. One real published row had an old-format slug; migrated it with a one-off script (not part of the app) - see SESSION_HANDOVER.md.
-4. `/services` gained a "Group by" tab row (Category / City / Community / County) - switching it re-groups the same table by that field instead of always Category, so each city/community/county shows up as its own expandable "sub service" section, per explicit user request.
-3. `Location.city` is now ALSO optional (was required), per explicit follow-up request - a Location just needs a Province plus at least one of City/Community/County, since generation is meant to combine "whichever of them is set". New `app/lib/location.ts` (`combineLocationName`, `primaryLocationName`) centralizes the "pick/combine whichever fields are set" logic, used for slugs, prompts (backend), confirm dialogs, modal titles, and the public page. Found and fixed a real bug during verification: `renderPlaceholders` (`app/lib/placeholders.ts`) rendered `{{City}}` as the literal text "null" on a row with no city set (JS stringifies `null` when passed to `.replace()`) - now defaults every field to `''`.
-2. Location gained separate `community`/`county` columns (both optional) alongside `city`/`province`, per explicit user request ("pisahkan kolom table untuk city, comunity dan county"). Every table showing location data (`/master-data`, `/generate-content`, `/import`, `/services`) now has 3 separate City/Community/County columns instead of a combined one; CSV upload, search, CSV export, and prompt variable substitution (`{{community}}`/`{{county}}`) all updated to match.
-1. Programmatic pages are now REAL public routes, not just an internal status. `MasterData.slug` (unique) is computed and persisted at import time; `/[city]/services/[category]/[service]/page.tsx` serves the actual page (real `<title>`/meta description via `generateMetadata`, renders stored HTML content), 404s for anything not published. The admin Sidebar/TopHeader are hidden specifically for that URL shape via a new `AppShell` component wrapping `layout.tsx`. This supersedes the earlier "publish is internal-only, no public route" note below - the user explicitly asked for real pages this time.
0. Fixed real AI generation: the Generate Content dropdown's model values ('claude-3-5-sonnet', 'gemini-1-5-pro') were never valid provider model IDs, so every generation call failed and rows silently sat at status 'error'. Also discovered mid-fix that Google had fully retired `gemini-1.5-pro` - replaced it with `-latest` alias options ("Gemini Flash" / "Gemini Pro") verified working against the user's real key.
1. Fixed the Master Data CSV generate flow (`/master-data`): `{{City}}`/`{{Province}}` placeholders in Service CSV meta columns now render per-location, re-uploading a Service CSV updates its stored template instead of being silently ignored, and rows/categories can now be deleted.
2. Added a batch "Limit" to `/generate-content` so a run can be capped to N rows and continued later; fixed two BullMQ worker bugs that were blocking that (per-batch progress, a job-completion update that referenced a non-existent field). Also added a Category picker so the user chooses which category to generate instead of it being implicit.
3. Added `MasterData.published`/`publishedAt` and a publish workflow, split into two menus per the user's explicit request: `/import` (generated content not yet published, with import actions) and `/services` (published content, browsed via a "Select Service" dropdown, with a preview-only Slug column previewing the eventual `/{city}/services/{category}/{service-name}` URL structure). Both share `GET/POST /api/publish`.

## Completed
- Next.js UI pages (`/`, `/master-data`, `/generate-content`, `/settings`).
- Database schema and PostgreSQL integration via Prisma.
- Monorepo restructuring (`apps/frontend`, `apps/backend`, `packages/database`).
- Redis and BullMQ integration for background AI processing.
- Dynamic prompt injection (`{{city}}`, `{{province}}`, `{{service_name}}`, `{{category}}`).
- Job Pause/Resume/Stop logic.
- Master Data generate: `{{City}}`/`{{Province}}` interpolation in Meta Title/Description/Heading/Subheading, service-template refresh on re-upload, tolerant CSV header parsing (whitespace/BOM), and clear zero-result feedback in the UI.
- Master Data delete: per-row and per-category deletion, both API (`DELETE /api/master-data`) and UI.
- Generate Content batch limit: cap a run to the next N pending rows via a new "Limit" field; Start button becomes "Continue Generation (N remaining)" for subsequent batches. Backend worker batches deterministically (`orderBy: id asc` + `take: limit`) and reports progress per-batch.
- Generate Content category picker: dropdown showing pending/total per category, drives the same start/limit/continue flow.
- Publish workflow, split across two menus (both use `GET/POST /api/publish`; `MasterData` gained `published`/`publishedAt`/`slug` columns):
  - `/import` — generated-but-unpublished content, grouped by category, with per-row/per-category/all import and a content preview modal.
  - `/services` — published content, filtered via a "Select Service" dropdown (defaults to "All Services"), grouped by category, with per-row unpublish, CSV export, and a "Slug"/"View Page" link to the real programmatic page (see below).
  - The original combined `/publish-content` page was removed in favor of this split (explicit user request).
- Programmatic pages are real routes now: `app/[city]/services/[category]/[service]/page.tsx` looks up `MasterData` by its stored `slug` (must be `published`), 404s otherwise, sets `<title>`/meta description from the rendered Meta Title/Description, and renders the stored HTML `content`. `AppShell` (`app/components/AppShell.tsx`) hides the dashboard Sidebar/TopHeader specifically for that URL pattern; every other route keeps the normal dashboard chrome.
- Repo pushed to GitHub: `https://github.com/rakawebmlg-ai/venture27.git` (`main`), with root `.gitignore` excluding `node_modules/`, `.env*`, `.next/`.

## In Progress
- None.

## Pending
- Authentication system (if required).
- Deployment pipeline to production (Netlify/Vercel/Docker).
- Real SMTP email dispatch upon job completion (currently setting is saved but email is not actually sent).

## Blocked
- None.

## Recently Modified Areas
- `packages/database/prisma/schema.prisma` (added `Job.generatedCount`/`Job.errorCount`, `Int @default(0)` - see item -15 above).
- `apps/backend/src/index.ts` (`runGeneration` increments `generatedCount`/`errorCount` per item alongside `progress`).
- `apps/frontend/app/generate-content/page.tsx` (new state for the two counts, wired through `reattachActiveJob`/`startGeneration`/the polling loop, rendered under the progress bar).
- `apps/frontend/app/globals.css` (teal re-theme - see item -12 above; `--color-blue-*`/`--color-accent*` values changed, variable names kept; `.sidebar-brand-icon`/`.sidebar-brand-text h1` rules removed, new `.sidebar-brand-logo`).
- `apps/frontend/app/components/Sidebar.tsx` (real logo image instead of "V27" text badge).
- `apps/frontend/app/result-guide/page.tsx` (one hardcoded blue rgba literal recolored to match).
- Moved `apps/frontend/public/service-page/venture27-logo.png` → `apps/frontend/public/venture27-logo.png` (shared between admin sidebar and the public page now).
- `apps/frontend/app/[type]/services/[value]/[service]/[heading]/page.tsx` (full visual redesign to match the Figma mockup - see item -10 above; still reads `MasterData`/`location`/`service`/`category` exactly as before, only the JSX/CSS changed; item -11 added the "Request Estimate" form + fixed a footer-logo aspect-ratio bug; item -12 above updated its logo `img src` path).
- New `apps/frontend/public/service-page/` (icon assets downloaded from Figma; the logo that used to live here moved to the shared top-level path above).
- New `/Users/nogariantana/File/.mcp.json` (Figma MCP server config, project-scoped to the actual workspace root - NOT inside this repo).
- `apps/frontend/app/api/generate/route.ts` (`GET` with no `jobId` now returns the latest `running`/`paused` `Job` instead of 400ing, for reattachment).
- `apps/frontend/app/generate-content/page.tsx` (new `reattachActiveJob()` called on mount alongside `fetchData`/`fetchPrompts`, rehydrates `jobId`/`progress`/`isGenerating`/`isPaused` from a still-active job).
- `apps/frontend/app/api/master-data/route.ts` (placeholder rendering, service template upsert, CSV parsing robustness, richer POST response, DELETE endpoint; placeholder renderer factored out to `app/lib/placeholders.ts`).
- `apps/frontend/app/master-data/page.tsx` (informative post-generate alert, delete UI).
- `apps/frontend/app/generate-content/page.tsx` (Limit input, Continue-labeled button, Category picker).
- `apps/frontend/app/api/generate/route.ts` ('start' now accepts/forwards `limit`).
- `apps/backend/src/index.ts` (batched, deterministic pending-item fetch; per-batch progress; fixed job-completion update).
- `apps/backend/src/index.ts` (`MODEL_IDS` map translates the UI's short `aiModel` value to the real provider model ID for all three providers; Gemini options updated to `-latest` aliases).
- `apps/frontend/app/generate-content/page.tsx` (AI Model dropdown: "Gemini 1.5 Pro" replaced with "Gemini Flash" + "Gemini Pro", both `-latest`; Preview Content wired up; error messages surfaced; prompt textarea always editable; per-row/per-category Reset buttons).
- `apps/frontend/app/api/master-data/route.ts` (new `PATCH` action `'reset'`).
- New `PromptTemplate` model (`packages/database/prisma/schema.prisma`) + `apps/frontend/app/api/prompts/route.ts` (GET/POST upsert-by-name/DELETE); `/generate-content` gained a Saved Prompts dropdown + Save/Delete.
- `MasterData.slug` (unique) + `app/[city]/services/[category]/[service]/page.tsx` (new public page route) + `app/components/AppShell.tsx` (new) + `apps/frontend/app/layout.tsx` (now just wraps `<AppShell>`) + `apps/frontend/app/lib/slug.ts` (`buildSlugPreview` renamed to `buildSlug`) + `apps/frontend/app/api/publish/route.ts` (import now computes/persists a slug per row instead of a single `updateMany`).
- `packages/database/prisma/schema.prisma` (added `MasterData.published` / `publishedAt`).
- New: `apps/frontend/app/import/page.tsx`, `apps/frontend/app/services/page.tsx`, `apps/frontend/app/api/publish/route.ts`, `apps/frontend/app/lib/placeholders.ts`, `apps/frontend/app/lib/slug.ts`.
- `apps/frontend/app/components/Sidebar.tsx` (added "Import" and "Service" nav items under Post-Generation; the earlier single "Publish Content" entry no longer exists).
- Removed `apps/frontend/app/publish-content/page.tsx` (split into `/import` + `/services`).
- `packages/database/prisma/schema.prisma` (`Location.community`/`county` added, then `Location.city` made optional too - all three are now individually optional, only `province` is required).
- New `apps/frontend/app/lib/location.ts` (`combineLocationName`, `primaryLocationName`); `apps/frontend/app/lib/placeholders.ts` bug fix (null City no longer renders as literal "null"); City/Community/County split into separate columns on `/master-data`, `/generate-content`, `/import`, `/services`.

## Current Technical Context
- Restoring the DB from scratch: `docker compose down -v && docker compose up -d`. The `-v` is what makes it work — without wiping `pgdata`, Postgres skips `docker-entrypoint-initdb.d` entirely and the dump is ignored. To refresh the dump itself, rebuild/re-pull `rakawebmlg/venture27-dbdump:latest` (`docker compose pull dbdump`); to take a new one, `docker exec venture27-db pg_dump -U postgres -Fc venture27 > venture27.dump`.
- The project successfully runs via `docker compose up -d` (for DB/Redis) followed by `npm run dev --workspace=@venture27/frontend` and `npm run dev --workspace=@venture27/backend`.
- Verified locally this session: Docker containers `venture27-db` (Postgres) and `venture27-redis` were already running; frontend dev server on port 3000, backend health server on port 3001. Master Data generate/delete flows and the Generate Content batch-limit flow were all tested end-to-end via direct API calls against the live DB, using throwaway test categories and (for the AI generation test) a deliberately invalid OpenAI key so no real spend occurred. Test data and the temporary key were cleaned up afterward; the user's real 260-row "Service 1" category was untouched throughout.

## Current Errors
- None known. If a user reports a "View Page" / published slug 404ing, check whether it predates the `/{type}/services/{value}/{service}/{heading}` slug format (item -5 above) - anything published before that change has an old-format slug that needs recomputing (see the one-off migration approach in SESSION_HANDOVER.md; there's no in-app "recompute slug" button, would need a similar throwaway script or a real migration endpoint if this recurs often). RESOLVED as of 2026-07-25 by the dump restore (item -8 above): the empty-data situation described below no longer applies — `MasterData` is back to 260 rows and the `Location`/`Service` rows are referenced again. Kept for context: Confirmed (user asked why Overview showed "20 locations, 13 core services" with empty data): `Location`/`Service` rows were orphaned - 0 of the 20 Location rows and 0 of the 13 Service rows are referenced by any `MasterData` row, and `MasterData` itself is 0 rows. Fixed the Overview stat to count only in-use rows (see Known Problems below). The orphaned rows themselves are still there (not deleted, by design - Delete Category never touches Location/Service) and can be reused the next time a CSV with the same City/Province/Service Name is uploaded. Root cause of the original data loss is still just a strong guess (most likely the user's own "Delete Category" click on their live SEO category) - never confirmed with the user, not fixed/reverted (there's no undo).

## Known Problems (fixed this session, kept for context)
- Content Preview List's "Preview Content" button used to be a dead stub with no `onClick` - now opens a modal (meta title/description/content).
- Error rows used to just say "Error" with no reason - `MasterData.errorMessage` now stores the caught error (cleared on a successful retry) and the UI surfaces it via a badge tooltip + the preview modal ("View Error").
- There was no way to regenerate a row short of editing the DB directly (which is what earlier fixes in this session did manually). `PATCH /api/master-data` (action 'reset') plus per-row/"Reset Errors"/"Reset Category" buttons on `/generate-content` now do this from the UI.
- The prompt.md input only accepted a file upload and then hid the content - replaced with an always-visible, always-editable textarea (file upload still works, just populates it).
- Content Preview List's columns/headers now match the Master Data table exactly (City/Community, Province, Service Name, Meta Title, Heading, Content, Image) instead of a condensed "Service & Location" + "Content Status"/"Image Status".
- Prompts weren't saved anywhere - every session meant re-uploading or retyping. New `PromptTemplate` model + `/api/prompts` (upsert-by-name) let you save a prompt under a name and reload it later via a dropdown on `/generate-content`; uploading a .md/.txt file auto-saves it under its filename.
- Generated content looked incomplete/garbled in preview. Two causes, both fixed in `apps/backend/src/index.ts`: (a) `maxTokens: 1500` was too low once Gemini's "thinking" models started spending most of that budget on invisible reasoning tokens (confirmed live: 1208/1500 spent on thinking, response cut off mid-sentence) - raised to 8000. (b) the worker only substituted `{{city}}`/`{{province}}`/`{{service_name}}`/`{{category}}`, but real-world prompt.md files use `{{City/Community}}`, `{{Service Name}}`, `{{Meta Title}}`, `{{Meta Description}}`, `{{Heading}}`, `{{Subheading}}`, `{{No}}` - unmatched variables were passed to the AI model literally, producing confused output. Added substitutions for all of them.
- Generation could hang at "running"/0% forever with no explanation if the worker failed before the per-item loop started (e.g. selected provider's API key not configured) - the throw only reached BullMQ's internal logging, never the Job DB row. Now wrapped in try/catch that sets `Job.status = 'failed'` + `Job.errorLogs`, surfaced to the user via an alert on the frontend poll.
- `/services` used to auto-select the alphabetically-first service name once data loaded, so after importing content spanning many services, only one of them showed by default - looked like "Import All" had dropped most of the content when everything had actually imported fine. Now defaults to "All Services".

## Known Problems
- The `generate-content` UI polls for job progress. WebSockets could be more efficient in the future.
- `npx prisma db push` / `prisma generate` in `packages/database` fails with `EPERM` on the query engine `.dll` while dev servers are running (Windows file lock). Stop `npm run dev` first if you need to regenerate the Prisma client after a schema change.
- `apps/backend`'s `npm run dev` (`tsx src/index.ts`, no watch mode) does not hot-reload on save - must be manually restarted after editing `apps/backend/src/index.ts`.
- `Location`/`Service` rows persist as reusable templates even after all their `MasterData` combinations are deleted (Delete Category, Reset, etc. never touch them) - by design, so re-uploading the same City/Province/Service Name later reuses the row instead of duplicating. This means `Location.count()`/`Service.count()` alone can overstate what's actually in use; `/api/overview` now filters to `data: { some: {} } }` for that reason - keep that pattern in mind for any other place that might naively count these tables.
- Pause/Resume on `/generate-content` is still broken (`resume` never re-enqueues a BullMQ job - see SESSION_HANDOVER.md). The new Limit/Continue flow avoids relying on it.
- Force-killing the backend process mid-job (e.g. `Stop-Process -Force` to apply a code change while a generation was running) causes BullMQ's stalled-job recovery to redeliver that job on the next worker start, which re-runs `runGeneration` from the top - `Job.status` gets reset to `'running'` unconditionally at the start of that function, so a job the user had explicitly clicked "Stop" on can come back to life after a backend restart, and progress can visibly regress (recalculated against the shrinking pending pool each redelivery) before catching back up. Observed directly while testing this session; not fixed (edge case tied to killing the dev process mid-run, not something that happens in normal use) - if it recurs, the fix is to check `Job.status !== 'stopped'` before that reset.
  - RECONFIRMED 2026-07-25 (item -9 above): this is not just a rare edge case - it fired from an ordinary `docker compose stop` + later `npm run dev --workspace=@venture27/backend` restart during unrelated work, and resumed the user's real `running` job (with real API calls against their real key) with no prompt or warning. Still not fixed. If picking this up: the safe fix really is checking `Job.status !== 'stopped'` (and probably `!== 'paused'`) before the unconditional reset in `runGeneration`, so only jobs that were genuinely still `running` at kill-time resume automatically.
- `apps/backend/src/index.ts` line ~4 has a pre-existing (not caused by recent work) TS error: `import { prisma, MasterData } from '@venture27/database'` - `MasterData` has no exported member. Harmless at runtime (`tsx` doesn't type-check), but worth cleaning up (unused import).
- The `gemini-pro-latest` model has `limit: 0` free-tier quota on the user's key (confirmed via the real API - `generativelanguage.googleapis.com` quota metric for `model: gemini-3.1-pro` is 0); `gemini-flash-latest` works fine and is what the free tier actually supports. If a user picks "Gemini Pro" and every row errors, that's expected on a free-tier key, not a bug - the error message (now shown in the UI) will say "quota exceeded" when this is the cause.

## Next Recommended Action
- Implement the actual SMTP email sending logic in the BullMQ worker when a job reaches 100%.
- `/result-guide` is now doubly stale: it was already a static mockup describing a different URL shape, and item -6 above just implemented a REAL sitemap/robots.txt system that makes that page's hardcoded "sitemap-index.xml, state-1-10000.xml, ..." example content redundant/inconsistent with what's actually being served. Worth rewriting it to describe the real, current system instead of a fictional one.
- Fix or remove the Pause/Resume buttons on `/generate-content` since Resume is currently a no-op.
- Set a real `SITE_URL` env var before deploying anywhere other than localhost, or canonical/OG URLs and metadataBase-resolved links will point at `http://localhost:3000`.

## Important Notes For The Next AI
- The codebase was just refactored into a Monorepo. Do NOT attempt to move it back to a standard structure.
- Always run `npm install` at the root, and make sure Prisma client is generated (`npm run build --workspace=@venture27/database`) — but stop any running dev servers first on Windows or `prisma generate` will EPERM.
